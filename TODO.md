# TODO

- [x] Inspect `src/app/horoscopes/[sign]/page.tsx` and identify merge-conflict markers.
- [ ] Resolve merge conflict by removing markers and keeping the HEAD implementation (getHoroscopeBySign + AstroColumnarLayout + helpers).
- [ ] Run `next build` (or `next lint`) to confirm the build error is resolved.
- [ ] If new errors appear, fix remaining merge conflicts/build issues.

